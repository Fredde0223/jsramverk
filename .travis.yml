language: node_js

addons:
    firefox: "60.0"

node_js:
    - "node"
    - "10"

sudo: false

git:
    submodules: false

before_script:
    - npm install
    - npm start

script:
    - npm test

after_success:
    - npm run report-coveralls
    - npm run report-codecov
